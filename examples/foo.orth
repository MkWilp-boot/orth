@define MAX_CAP i 900
@define SOME_STR s "Hello world from pre-directive\n"

proc new_proc in
    SOME_STR puts
    i 90 exit
end

proc main in
    i32 10 i32 20 + putui s "\n" puts
    invoke new_proc
    MAX_CAP putui
end