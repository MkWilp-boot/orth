@include "std.orth"

proc main in
    i 10 i 10 + i 20 == if
        invoke p_if
    else
        invoke p_else
    end
    
    var msg = s "I am inside the main entry point!!!"
    hold msg put_string
end